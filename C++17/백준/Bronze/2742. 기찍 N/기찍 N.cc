#include <iostream>
#include <iterator>
#include <string>
using namespace std;

int main() {

    int N;

    cin >> N;

    for (int i = 1; i <= N; N--) {
        cout << N << "\n";
    }

    return 0;
}